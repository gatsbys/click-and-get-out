{
  "name": "click-and-get-out",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check background.js && node --check content.js && node --check selectors.js && node --check popup.js",
    "pack": "node scripts/pack.mjs",
    "demo": "node scripts/demo.mjs",
    "shots": "node scripts/store-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
